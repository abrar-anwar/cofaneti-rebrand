<style>
  .shape-picker-sub {
    margin: 1rem 0;
    text-align: center;
  }

  .shape-picker__group {
    border: 0;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1rem;
  }

  .shape-picker__option {
    position: relative;
  }

  .shape-picker__input {
    position: absolute;
    opacity: 0;
    pointer-events: none;
  }

  .shape-picker__label {
    --shape-size: 100px; /* overall width */
    width: var(--shape-size);
    height: calc(var(--shape-size) * 0.8); /* adjust if shapes taller/shorter */
    display: inline-block;
    background: no-repeat center/contain var(--shape-img);
    cursor: pointer;
    position: relative;
    transition: transform 0.35s ease;
  }

  .shape-picker__group.type_none input[type="radio"] + label{
    margin: 0 !important;
  }

  .shape-picker__label:hover {
    transform: scale(1.03);
  }

  /* keyboard focus outline */
  .shape-picker__input:focus-visible + .shape-picker__label {
    box-shadow: 0 0 0 3px rgba(0, 0, 0, 0.6);
    border-radius: 4px; /* fallback square focus box */
  }

  /* selected: show text & highlight */
  {% comment %} .shape-picker__input:checked + .shape-picker__label {
    filter: drop-shadow(0 0 4px rgba(0, 0, 0, 0.25));
    outline: 2px solid #333;
    outline-offset: 4px;
  } {% endcomment %}

  .shape-picker__input:checked + .shape-picker__label::after {
    content: attr(data-label);
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    pointer-events: none;
    padding-top:20px;
  }

  /* Visually hidden utility (re‑use if you already have one) */
  .visually-hidden {
    position: absolute !important;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0 0 0 0);
    clip-path: inset(50%);
    white-space: nowrap;
    border: 0;
  }
  .shape-picker__group.product-form__input--pill.type_none input[type='radio'] + label,
  .shape-picker__group.product-form__input--pill.type_none input[type='radio']:checked + label {
    border-color: transparent !important;
    border-bottom: unset !important;
  }
</style>
<div class="shape-picker-sub">
  <fieldset class="shape-picker__group product-form__input--pill type_none">
    <legend class="form__label">{{ section.settings.model_title }}</legend>

    {%- comment -%} Shape 1 {%- endcomment -%}
    {% if section.settings.model1 != blank %}
      <div class="shape-picker__option">
        <input
          type="radio"
          name="properties[{{ section.settings.model_title }}]"
          id="shape-1"
          value="{{ section.settings.model1_name }}"
          class="shape-picker__input"
          checked
        >
        <label
          for="shape-1"
          class="shape-picker__label"
          data-label="{{ section.settings.model1_name }}"
          style="--shape-img:url({{ section.settings.model1 | image_url: width: 120 }});"
        ></label>
      </div>
    {% endif %}

    {%- comment -%} Shape 2 {%- endcomment -%}
    {% if section.settings.model2 != blank %}
      <div class="shape-picker__option">
        <input
          type="radio"
          name="properties[{{ section.settings.model_title }}]"
          id="shape-2"
          value="{{ section.settings.model2_name }}"
          class="shape-picker__input"
        >
        <label
          for="shape-2"
          class="shape-picker__label"
          data-label="{{ section.settings.model2_name }}"
          style="--shape-img:url({{ section.settings.model2 | image_url: width: 120 }});"
        ></label>
      </div>
    {% endif %}

    {%- comment -%} Shape 3 {%- endcomment -%}
    {% if section.settings.model3 != blank %}
      <div class="shape-picker__option">
        <input
          type="radio"
          name="properties[{{ section.settings.model_title }}]"
          id="shape-3"
          value="{{ section.settings.model3_name }}"
          class="shape-picker__input"
        >
        <label
          for="shape-3"
          class="shape-picker__label"
          data-label="{{ section.settings.model3_name }}"
          style="--shape-img:url({{ section.settings.model3 | image_url: width: 120 }});"
        ></label>
      </div>
    {% endif %}

    {%- comment -%} Shape 4 {%- endcomment -%}
    {% if section.settings.model4 != blank %}
      <div class="shape-picker__option">
        <input
          type="radio"
          name="properties[{{ section.settings.model_title }}]"
          id="shape-4"
          value="{{ section.settings.model4_name }}"
          class="shape-picker__input"
        >
        <label
          for="shape-4"
          class="shape-picker__label"
          data-label="{{ section.settings.model4_name }}"
          style="--shape-img:url({{ section.settings.model4 | image_url: width: 120 }});"
        ></label>
      </div>
    {% endif %}

    {%- comment -%} Shape 5 {%- endcomment -%}
    {% if section.settings.model5 != blannk %}
      <div class="shape-picker__option">
        <input
          type="radio"
          name="properties[{{ section.settings.model_title }}]"
          id="shape-5"
          value="{{ section.settings.model5_name }}"
          class="shape-picker__input"
        >
        <label
          for="shape-5"
          class="shape-picker__label"
          data-label="{{ section.settings.model5_name }}"
          style="--shape-img:url({{ section.settings.model5 | image_url: width: 120 }});"
        ></label>
      </div>
    {% endif %}
  </fieldset>
</div>
<hr>
